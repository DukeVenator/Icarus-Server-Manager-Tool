namespace IcarusProspectEditor.Services;

/// <summary>Initial UI values used for dirty-state baselines (must match control defaults).</summary>
internal static class ProspectEditorMetadataDefaults
{
    public const int ClaimedCharacterSlot = 0;
}
